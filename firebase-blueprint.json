{
  "entities": {
    "Dream": {
      "title": "Dream",
      "description": "A dream entry created by a user.",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The UID of the user who created this dream."
        },
        "prompt": {
          "type": "string",
          "description": "The original dream description provided by the user."
        },
        "story": {
          "type": "string",
          "description": "The expanded story generated by AI."
        },
        "createdAt": {
          "type": "timestamp",
          "description": "The time the dream was created."
        }
      },
      "required": ["userId", "prompt", "story", "createdAt"]
    }
  },
  "firestore": {
    "/dreams/{dreamId}": {
      "schema": {
        "$ref": "#/entities/Dream"
      },
      "description": "Stores user dreams."
    }
  }
}
